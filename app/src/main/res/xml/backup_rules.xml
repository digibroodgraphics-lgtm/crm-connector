<?xml version="1.0" encoding="utf-8"?>
<!--
    Legacy (API <= 30) backup configuration. We exclude everything to keep
    encrypted tokens and the local sync queue on-device only.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
